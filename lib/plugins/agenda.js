// Load modules
var agenda = require('hapi-agenda');

// Declare internals
var internals = {};

module.exports = function(hapi) {

  // var options = hapi.app.options;
  // hapi.register({
  //   register: agenda,
  //   options: {
  //     jsonApi: true,
  //     mongoUrl: 'localhost:27017/artishan',
  //     jobs: options.jobsDir, // path to directory containing job files
  //     processEvery: '5 seconds', // Defaults to 30 seconds. Lower numbers = higher db calls
  //     every: { // Runs these jobs
  //       '60 seconds': 'health'
  //     }
  //   }
  // }, function(err) {
  //   if(err) console.error(err);
  // });

};
